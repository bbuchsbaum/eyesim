% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gaze_weave_cv.R
\name{gaze_weave_cv}
\alias{gaze_weave_cv}
\title{Cross-fitted GazeWeave analysis}
\usage{
gaze_weave_cv(
  ref_tab,
  source_tab,
  match_on,
  contrast_on = NULL,
  refvar = "fixgroup",
  sourcevar = "fixgroup",
  spec = NULL,
  engine = NULL,
  split_on = match_on,
  n_folds = NULL,
  seed = NULL,
  fit_source_filter = NULL,
  eval_source_filter = NULL,
  episode_on = NULL,
  priorvar = NULL
)
}
\arguments{
\item{ref_tab, source_tab}{Reference and source tables.}

\item{match_on}{One or more columns defining the true template match.}

\item{contrast_on}{Optional columns restricting nonmatching candidates.}

\item{refvar, sourcevar}{List columns containing fixation groups.}

\item{spec}{A [gaze_transport_spec()] or [gaze_replay_spec()].}

\item{engine}{Either `"transport"` or `"replay"`. When omitted, it is
inferred from `spec`.}

\item{split_on}{Columns defining the held-out unit. Defaults to `match_on`.}

\item{n_folds}{Number of cross-fitting folds.}

\item{seed}{Fold-assignment seed. `NULL` uses the engine-specific default.}

\item{fit_source_filter, eval_source_filter}{Optional logical vectors or
functions selecting fitting and evaluation source rows.}

\item{episode_on}{Optional reference columns identifying separate study
presentations for Transport. Their likelihoods receive equal prior weight.}

\item{priorvar}{Optional Transport reference column containing one positive
design-prior weight per candidate.}
}
\value{
An engine-specific fitted object. Use `broom::tidy()` for the sole
  primary endpoint, `gaze_info_bits`.
}
\description{
`gaze_weave_cv()` is the common entry point for the two GazeWeave engines.
Use Transport for symmetric explanatory alignment and Replay for a
directional encoding-to-recall model. There is no universal engine default;
the supplied engine-specific specification selects the scientific model.
}
\details{
Both engines return held-out candidate probabilities and
`gaze_info_bits = log2(p_true / prior_true)`. Replay correspondence is a
posterior probability. Transport correspondence is an optimized alignment
and must not be interpreted as statistical uncertainty.
}
