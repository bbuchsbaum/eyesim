% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gaze_weave_transport_v3_cv.R
\name{gaze_transport_cv}
\alias{gaze_transport_cv}
\title{Nested-calibrated exhaustive-candidate Transport}
\usage{
gaze_transport_cv(
  ref_tab,
  source_tab,
  match_on,
  contrast_on = NULL,
  refvar = "fixgroup",
  sourcevar = "fixgroup",
  spec = gaze_transport_spec(),
  split_on = match_on,
  n_folds = NULL,
  seed = 20260822L,
  fit_source_filter = NULL,
  eval_source_filter = NULL,
  episode_on = NULL,
  priorvar = NULL
)
}
\arguments{
\item{ref_tab, source_tab}{Reference and source tables.}

\item{match_on}{One or more columns defining the true template match.}

\item{contrast_on}{Optional columns restricting nonmatching candidates.}

\item{refvar, sourcevar}{List columns containing fixation groups.}

\item{spec}{A [gaze_transport_spec()].}

\item{split_on}{Columns defining the held-out unit. Defaults to `match_on`.}

\item{n_folds}{Number of cross-fitting folds.}

\item{seed}{Fold-assignment seed. `NULL` uses the engine-specific default.}

\item{fit_source_filter, eval_source_filter}{Optional logical vectors or
functions selecting fitting and evaluation source rows.}

\item{episode_on}{Optional columns identifying the separate study
presentations within a candidate. Their likelihoods receive equal prior
weight and are never concatenated.}

\item{priorvar}{Optional reference-table column containing one positive
design-prior weight per candidate. `NULL` declares a uniform design.}
}
\value{
A `gaze_transport_fit` with held-out candidate probabilities,
  fold receipts, response-blind quality diagnostics, and calibration checks.
}
\description{
Outer folds hold out participant-item cells. Within each outer-training set,
warp fitting and episode-scale temperature/reliability calibration are
repeated on item-grouped inner folds. Every held-out row is scored against
every permitted candidate in its contrast stratum with the actual declared
candidate prior and one common solver, warp, and equal-episode policy.
}
