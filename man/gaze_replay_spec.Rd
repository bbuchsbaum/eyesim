% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gaze_weave_replay.R
\name{gaze_replay_spec}
\alias{gaze_replay_spec}
\title{Specify the directional GazeWeave replay model}
\usage{
gaze_replay_spec(
  grid_size = 64L,
  max_skip = 2L,
  student_df = 4,
  scale_floor = 0.05,
  transition_grid = list(background = c(0.02, 0.08), restart = c(0.01, 0.05, 0.12),
    advance = c(0.15, 0.35), background_stay = c(0.85, 0.95)),
  temperature_bounds = c(0.05, 100),
  reliability = c("none", "effective_fixations"),
  reliability_kappa_bounds = c(0, 100),
  warp = gaze_warp_none(),
  screen = NULL
)
}
\arguments{
\item{grid_size}{Number of normalized duration-mass observations per recall
trial.}

\item{max_skip}{Maximum forward encoding-state jump in a local transition.}

\item{student_df}{Degrees of freedom for robust bivariate Student emissions.}

\item{scale_floor}{Positive lower bound for fitted spatial scales.}

\item{transition_grid}{Named list of candidate probabilities for background
entry, chunk restart, local advance, and background persistence.}

\item{temperature_bounds}{Positive lower and upper bounds for training-only
calibration of candidate likelihoods. Direct one-template fits retain
temperature one because no contrastive calibration set exists.}

\item{reliability}{Response-blind reliability policy. `"none"` preserves
the calibrated candidate posterior. `"effective_fixations"` learns a
monotone shrinkage toward the declared candidate prior from the source
path's duration-effective fixation count.}

\item{reliability_kappa_bounds}{Non-negative bounds for the reliability
half-saturation parameter. The lower bound must be zero.}

\item{warp}{Cross-fitted warp specification.}

\item{screen}{Optional screen geometry.}
}
\value{
A frozen `gaze_replay_spec`.
}
\description{
Replay models a recall path as a normalized gaze-time sequence generated by
local traversal of encoding fixations, occasional chunk restarts, and a
candidate-independent background state. Transition and emission parameters
are selected using training trials only by [fit_gaze_replay_model()].
}
