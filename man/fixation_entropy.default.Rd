% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/entropy.R
\name{fixation_entropy.default}
\alias{fixation_entropy.default}
\alias{fixation_entropy.eye_density}
\alias{fixation_entropy.density}
\alias{fixation_entropy.eye_density_multiscale}
\alias{fixation_entropy.fixation_group}
\title{Entropy of fixation patterns}
\usage{
\method{fixation_entropy}{default}(x, ...)

\method{fixation_entropy}{eye_density}(x, normalize = TRUE, base = exp(1), ...)

\method{fixation_entropy}{density}(x, normalize = TRUE, base = exp(1), ...)

\method{fixation_entropy}{eye_density_multiscale}(
  x,
  normalize = TRUE,
  base = exp(1),
  aggregate = c("mean", "none"),
  ...
)

\method{fixation_entropy}{fixation_group}(
  x,
  normalize = TRUE,
  base = exp(1),
  method = c("density", "grid"),
  sigma = NULL,
  xbounds = NULL,
  ybounds = NULL,
  outdim = c(50, 50),
  grid = c(10, 10),
  duration_weighted = FALSE,
  ...
)
}
\arguments{
\item{x}{The input object.}

\item{...}{Additional arguments passed to `eye_density()` when
`method = "density"`.}

\item{normalize}{Logical; if `TRUE` (default), divide entropy by the maximum
possible entropy for the number of valid bins so results lie in `[0, 1]`.}

\item{base}{Logarithm base used in the Shannon entropy. Defaults to
`exp(1)`; use `2` for bits.}

\item{aggregate}{For multiscale density objects, one of `"mean"` (default)
or `"none"`.}

\item{method}{For `fixation_group` objects, one of `"density"` (default) or
`"grid"`.}

\item{sigma}{Optional bandwidth for density-based entropy on fixation groups.
If `NULL`, `suggest_sigma()` is used.}

\item{xbounds, ybounds}{Optional display bounds for fixation groups. If not
supplied, the observed fixation ranges are used with a small padding.}

\item{outdim}{Grid dimensions for density-based entropy from fixation groups.}

\item{grid}{Grid dimensions for occupancy-grid entropy from fixation groups.}

\item{duration_weighted}{Logical; if `TRUE`, duration-weighted KDE is used
for density-based entropy.}
}
\value{
A numeric entropy value. Multiscale density objects return either the
  mean entropy across scales or a named numeric vector, depending on
  `aggregate`.
}
\description{
These methods quantify how concentrated or diffuse a fixation pattern is
using Shannon entropy. For density maps, entropy is computed from the
normalized density surface. For fixation groups, entropy can be computed from
either a derived density map or a discrete occupancy grid.
}
