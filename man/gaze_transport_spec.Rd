% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gaze_weave_transport_v3_spec.R
\name{gaze_transport_spec}
\alias{gaze_transport_spec}
\title{Specify edge-normalized GazeWeave Transport}
\usage{
gaze_transport_spec(
  spatial = gaze_gaussian_mixture(c(0.75, 1.5), weights = c(0.7, 0.3)),
  chronology = gaze_order_neighbours(neighbours = 2),
  coverage_prior = c(2, 2),
  coverage_nodes = 12L,
  temporal_weight = 2,
  selection_weights = c(0.5, 0.5),
  entropy_schedule = c(0.05, 0.015),
  temperature_bounds = c(0.05, 20),
  warp = gaze_warp_none(),
  screen = NULL,
  maxit = 1000L,
  step_size = 2,
  tolerance = 5e-05,
  projection_maxit = 1000L,
  projection_tolerance = 1e-08,
  projection_method = c("auto", "standard", "log"),
  multistart = 1L,
  backend = c("auto", "optimized", "reference"),
  polish = c("none", "audit"),
  polish_maxit = 200L,
  polish_gap_tolerance = 1e-07,
  polish_relative_tolerance = 1e-08,
  reliability = c("effective_fixations", "none"),
  reliability_kappa_bounds = c(0, 100),
  calibration_folds = 2L,
  calibration_seed = 20260822L,
  log_temperature_prior_sd = 1,
  log1p_kappa_prior_sd = 1
)
}
\arguments{
\item{spatial}{A [gaze_gaussian_mixture()] specification.}

\item{chronology}{Ordinal chronology from [gaze_order_neighbours()].}

\item{coverage_prior}{Two positive beta-prior shape parameters.}

\item{coverage_nodes}{Number of default coverage quadrature nodes.}

\item{temporal_weight}{Non-negative directed local-order weight.}

\item{selection_weights}{Non-negative reference and source selection weights.}

\item{entropy_schedule}{Positive correspondence-smoothing continuation values.}

\item{temperature_bounds}{Calibration temperature bounds.}

\item{warp}{Candidate-invariant cross-fitted warp specification.}

\item{screen}{Optional screen geometry.}

\item{maxit, step_size, tolerance}{Reference solver controls.}

\item{projection_maxit, projection_tolerance, projection_method}{Fixed-mass
projection controls.}

\item{multistart}{One or two common structural starts.}

\item{backend}{Pair solver backend. `"reference"` is the readable R oracle;
`"optimized"` uses the estimator-preserving batched implementation;
`"auto"` uses the optimized backend with a clean reference fallback.}

\item{polish}{Optional scientific-objective Frank-Wolfe audit. The default
keeps the entropic solution; `"audit"` polishes every coverage node.}

\item{polish_maxit, polish_gap_tolerance, polish_relative_tolerance}{Conditional-gradient stopping controls.}

\item{reliability}{Response-blind calibration policy. The default learns
effective-fixation shrinkage on inner out-of-fold predictions and includes
the temperature-only solution as an exact boundary.}

\item{reliability_kappa_bounds}{Non-negative bounds for the shrinkage scale.}

\item{calibration_folds, calibration_seed}{Inner calibration-fold policy.}

\item{log_temperature_prior_sd, log1p_kappa_prior_sd}{Calibration penalties.}
}
\value{
A frozen `gaze_transport_spec`.
}
\description{
Transport separates matched coverage, normalized correspondence,
selected source and target mass, spatial fidelity, directed local order,
correspondence smoothing, and candidate-invariant registration. Coverage
is integrated by Gauss-Legendre quadrature under a fixed beta prior.
}
